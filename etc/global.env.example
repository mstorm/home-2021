TZ=Asia/Seoul

# DNS domain name (e.g., home.internal)
DNS_NAME=home.internal

# Let's Encrypt / ACME
LE_EMAIL=mstorm@mstorm.net

# Cloudflare DNS-01 token (Zone.DNS:Edit)
CLOUDFLARE_DNS_API_TOKEN=__REPLACE_ME__

# Cloudflare Tunnel token
TUNNEL_TOKEN=__REPLACE_ME__
