# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=__REPLACE_ME__
POSTGRES_DB=postgres

# Zitadel database (optional - for automatic initialization)
# If set, Zitadel database and user will be created automatically
ZITADEL_DATABASE_PASSWORD=__REPLACE_ME__
