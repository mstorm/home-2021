# Watchtower configuration
# Check interval (default: 24h)
# Examples: 30s, 5m, 1h, 24h
WATCHTOWER_INTERVAL=24h

# Additional watchtower options can be added via command in compose.yml
# Common options:
#   --cleanup              Remove old images after update
#   --include-stopped      Include stopped containers
#   --monitor-only         Only monitor containers with watchtower label
#   --label-enable         Only update containers with com.centurylinklabs.watchtower.enable=true label

