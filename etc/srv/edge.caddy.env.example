# Let's Encrypt / ACME
LE_EMAIL=mstorm@mstorm.net

# Cloudflare DNS-01 token (Zone.DNS:Edit)
# For caddy-docker-proxy, use CLOUDFLARE_DNS_API_TOKEN
CLOUDFLARE_DNS_API_TOKEN=__REPLACE_ME__

# Caddy configuration
# Optional: Custom Caddyfile path (if not using Docker labels)
# CADDYFILE=/path/to/Caddyfile

