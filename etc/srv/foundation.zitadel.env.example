# ZITADEL - fill based on your chosen ZITADEL config (version-specific).
# Example placeholders:
ZITADEL_HOST=id.home.internal
ZITADEL_INTERNAL_PORT=8080

# Database (use your postgres)
ZITADEL_DATABASE_HOST=postgres
ZITADEL_DATABASE_PORT=5432
ZITADEL_DATABASE_USER=zitadel
ZITADEL_DATABASE_PASSWORD=__REPLACE_ME__
ZITADEL_DATABASE_NAME=zitadel

# Master key / secrets (fill)
# Generate a 32-byte master key for encrypting sensitive data:
#   echo "$(tr -dc A-Za-z0-9 </dev/urandom | head -c 32)"
# WARNING: Once set, changing this key will make encrypted data inaccessible!
ZITADEL_MASTERKEY=__REPLACE_ME__
