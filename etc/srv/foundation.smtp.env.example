# Postfix SMTP configuration
# SMTP port (default: 25)
SMTP_PORT=25

# Postfix hostname
SMTP_MYHOSTNAME=smtp.home.mstorm.net

# Postfix domain
SMTP_MYDOMAIN=home.mstorm.net

# Postfix origin (default: $mydomain)
# SMTP_MYORIGIN=$mydomain

# Allowed sender domains (comma-separated, empty = allow all)
# Example: example.com,example.org
SMTP_ALLOWED_DOMAINS=

# Relay host configuration (optional)
# If set, Postfix will relay emails through this host
# Example: [smtp.gmail.com]:587
SMTP_RELAYHOST=

# Relay host authentication (if required)
# SMTP_RELAYHOST_USERNAME=your-email@gmail.com
# SMTP_RELAYHOST_PASSWORD=your-app-password

# Relay host TLS (default: yes)
# SMTP_RELAYHOST_TLS=yes

# Additional Postfix configuration
# You can add custom Postfix settings via environment variables
# Format: POSTFIX_<setting_name>=<value>
# Example: POSTFIX_mynetworks=127.0.0.0/8,172.16.0.0/12

