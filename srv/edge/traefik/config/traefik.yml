api:
  dashboard: true
  insecure: true
  disableDashboardAd: true

providers:
  docker:
    exposedByDefault: false
    network: net_external
    endpoint: unix:///var/run/docker.sock
  file:
    directory: /config
    watch: true

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
